import Login from './Login';
import MainPage from './MainPage';
import './index.css'

function App() {

  const loginSuccess = (email) => {
    
  }

  return (
    <div className="App">
      <Login/>
    </div>
  );
}

export default App;
